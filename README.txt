There are two files you will need in the project's root directory:
  1. config.json -- contains your Canvas API token (you'll need this for sure)
  2. StudentExtensions.xlsx -- list of extensions for the student extension feature (extra time on quizzes, etc.)
